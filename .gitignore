node_modules/
.env
dist/
.DS_Store

# Local dev vault (created by seed script / task dev)
dev-vault/
package-lock.json

# Rust build artifacts
target/

# Local AI context — kept off source control (contains internal notes)
CLAUDE.md
.mcpregistry_github_token
.mcpregistry_registry_token
